import React from "react";
import {Grid} from "@mui/material";

const Course = (props) => (
    <Grid container>
        <Grid item xs={12} p={1}>
            1 EUR ≈ 3.41 LTL
        </Grid>
    </Grid>
);

export default Course;
